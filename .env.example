# PermitWise Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
PORT=5000
NODE_ENV=development

# Database (Required - get from Railway or MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/permitwise

# JWT Secret (Required - generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Stripe (Required for payments)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_STARTER=price_starter_id
STRIPE_PRICE_PRO=price_pro_id
STRIPE_PRICE_ELITE=price_elite_id

# Twilio (Optional - for SMS alerts)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Email SMTP (Required for email verification and alerts)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# URLs (Update for production)
# For local development:
# BASE_URL=http://localhost:5000
# CLIENT_URL=http://localhost:3000

# For Railway production, set these in Railway dashboard:
BASE_URL=https://permitwisedotappproduction-production.up.railway.app
CLIENT_URL=https://permitwisedotappproduction-production.up.railway.app

# Admin Credentials (Required for admin panel access)
# These are the master admin login credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=AshleyJace2025!

# Admin
ADMIN_SECRET=your-admin-secret-for-seeding

# Railway Deployment Notes:
# 1. Set all variables in Railway dashboard under "Variables"
# 2. MONGODB_URI: Use Railway's MongoDB plugin or MongoDB Atlas
# 3. NODE_ENV: Set to "production" in Railway
# 4. BASE_URL and CLIENT_URL: Both should be https://permitwisedotappproduction-production.up.railway.app
